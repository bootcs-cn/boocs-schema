# Bootcs Schema

Bootcs 课程和关卡配置的 JSON Schema 定义。

## 📋 Schema 文件

- **`course.schema.json`** - 课程配置 (`course.yml`)
- **`stage.schema.json`** - 关卡配置 (`stage.yml`)
